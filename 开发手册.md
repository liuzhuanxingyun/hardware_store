
1.重装一下系统，centos，记得记录root密码
1.1研究如何进入到根目录有规划的管理好依赖文件夹
2.安装python3.9
3.安装nginx
4.安装mysql，记得修改root密码并记录，另外记得检查端口开放
5.编写uwsgi配置文件（提升性能使用，防止一卡就完全卡死）

7.导出requirements依赖文件
8.压缩为zip，将后端文件传到服务器上
9.nginx的配置文件更改后，要重启
10.创建数据库，安装依赖，导入对应数据
11配置admin访问管理后台（不明白意义何在）
12.准备好自己的域名，备好案将证书放到服务器上
13.小程序后台配置合法地址

同步就是前一个走完才到下一个
异步就是这一步没走完可以先做别的，等有结果了再回来




！！！（后端）开发流程：

首先创建django模版
cd backend

conda activate hardware_store

python manage.py migrate  
# 初始化数据库（创建表结构）

python manage.py runserver 0.0.0.0:8000
<!-- 到这里才算启动成功 -->

django-admin startproject hardware_store /Users/david/Documents/co-work/hardware_store/backend

python manage.py startapp hardware_app
<!-- 上二则是初始化设定 -->

首先要明白前后端交互分为三步
1.首先前端要有一个空变量，或者空数组这样的【空】形参，在js文件中定义该参数
2.其次设计一个函数，获取后端数据，具体包括“从哪一个【URL】获取”等等
3.最后要做一个【接收函数】，接收网站传给你的信息

🫵🫵🫵首先创建model数据库类型，并定义结构
其次做表迁移：
# cd backend
# conda activate hardware_store
# python manage.py makemigrations
# python manage.py migrate
# python manage.py runserver 0.0.0.0:8000
再到setting处设置路径并且在urls处设置路由（这个并非每次都要，设置好了就不用）


🫵🫵🫵记得到app下的admin注册一下表「之后就可以在上面上传图片了」
创建超级用户root上传图片（这个并非每次都要，设置好了就不用）
<!-- python manage.py runserver 0.0.0.0:8000 -->
🫵🫵🫵然后在admin中录入数据
http://0.0.0.0:8000/admin/login/?next=/admin/
http://10.44.43.168:8000/admin/login/?next=/admin/


🫵🫵🫵然后在app的view写好接口和urls文件。
以及总的urls文件写好接口，使得前端可以获取









待办：：：：

在js中添加onsharetimeline，允许分享到朋友圈，

需要处理分页加载（onReachBottom），当用户滑到底部时加载更多商品。
点击商品卡片需要跳转到一个新的页面，
展示商品大图、详细参数、富文本描述（HTML内容）以及“加入购物车”和“立即购买”的底部栏。

五金产品通常涉及安装和规格咨询

要求简明好用，延续home的风格






商品页面，应该是，每点进去一个商品，就通过一个good页面，然后渲染对应的这个商品信息，包括图片

分类页面暂时还没有数据，到时候让他们拍好照再统一录入。

购物车页面要有删除商品的选项，也要用顶部导航栏，
我的页面删去联系客服，意见反馈，设置的选项。

🐷建立个人数据库，完善登录页面

加入注册选项（不需要，要注册来也没啥用）

我想要探讨一个方案：
1.该方案利用微信的openid，在用户进入小程序的时候就进行身份认证，然后与购物车等信息绑定在一起，但是我不确定微信的openid要不要钱，会不会超过多少次要付费
2.该方案利用我在后台设置的账号密码进行登录，然后与购物车等信息绑定在一起，
因为整个项目其实应该只有很少人用，只是在实体店帮助计算的一个小程序，现在也没有接入支付功能的打算，只是为了更方便给用户展示五金店各种繁杂的商品，我更想用方案一，你觉得可行吗

在首页的热销推荐以及goods_details页面帮助完善“加入购物车逻辑”，使得物品可以加入购物车，是不是应该在后端建立一个独属于这个用户的购物车数据库？

-----如果自动登录成功，mine页面应该显示其账户名，原来的登录功能也就不需要了

记得做这个快速跳转区的图片后端获取
购物车价格最多显示小数点后三位数








mine中，收货地址点进去一个新页面，可以填写收货地址和人，通讯方式的东西的记录，可以保留多个，收货地址，可以自由选择默认收货地址，这样在购物车结算的时候就可以显示出来

order_confirm页面需要优化，右边都卡出界面外了。

系统需强制用户在登录后才能进行下单与购买操作，所有用户行为数据需与其账号进行绑定并长期保存，包括商品信息、下单记录、收货地址等完整历史数据，实现用户数据可追溯与可记忆；在结算阶段弹出标准结算页面，用户填写或自动读取默认订货信息（如常用地址、联系人等），提交订单后无需支付，但系统需将整理后的完整订单数据实时发送至后台数据库进行存档与后续处理。



